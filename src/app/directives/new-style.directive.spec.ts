import { NewStyleDirective } from './new-style.directive';

describe('NewStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new NewStyleDirective();
    expect(directive).toBeTruthy();
  });
});
